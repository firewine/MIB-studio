# Mockup Status

The canonical v0 UX artifact is `260620 FE mockup.html`.

Other HTML files in this folder are archived design iterations. They may be useful for visual comparison, but implementation must follow `docs/specs/UX_SPEC.md` and the canonical mockup when there is any conflict.

Rules for archived mockups:

- They must not introduce v0 scope beyond Router.
- Any benchmark-like numbers must be labeled as example/unverified or explicitly not benchmark evidence.
- Model names should use the locked v0 catalog names (`google/gemma-2b-it`, `microsoft/Phi-3.5-mini-instruct`) or be marked v0.2+ disabled.
